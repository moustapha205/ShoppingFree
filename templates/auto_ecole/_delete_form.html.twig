<form method="post" action="{{ path('app_auto_ecole_delete', {'id': auto_ecole.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ auto_ecole.id) }}">
    <button class="btn">Delete</button>
</form>
